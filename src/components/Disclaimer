export default function Disclaimer() {
  return (
    <div style={{
      background: 'var(--gold-pale)',
      border: '1px solid var(--border)',
      borderRadius: 8,
      padding: '14px 18px',
      marginBottom: 26,
      fontFamily: 'Inter, sans-serif',
      fontSize: 12.5,
      color: 'var(--ink-light)',
      lineHeight: 1.6,
    }}>
      <strong style={{ color: 'var(--ink)' }}>The Berean Standard.</strong>{' '}
      Every study here is a starting point, not a final word. Hymn lyrics quoted
      in full are confirmed public domain; for newer songs, only short excerpts
      are quoted with a link to the full text. Cross-references are drawn from
      the Treasury of Scripture Knowledge (CC-BY OpenBible.info). Open your own
      Bible and hymnal alongside what's generated here — and verify before you teach it.
    </div>
  )
}
